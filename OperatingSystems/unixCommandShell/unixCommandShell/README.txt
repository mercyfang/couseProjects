This projects aims to write a simplified Unix command interpreter in C, similar to bash or csh in purpose.

By typing make in the terminal, makefile will complie source.c which is the c source file. File souce will be generated. This file is also in the directory but it does not necessarily be there because it could also be generated by typing make . Then typing ./source in the terminal, the program source will start to run. profile includes PATH and HOME information which is needed for data initializaiton.
